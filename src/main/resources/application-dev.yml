# 로컬 설정 파일

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:test'
    username: username
    password: password

  h2:
    console:
      enabled: true
      path: /h2-console